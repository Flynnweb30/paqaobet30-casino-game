# Paqaobet30

Responsive demo-only slot game with virtual credits.

## Local
npm install
npm start

Open http://localhost:10000

## Render
Create a Web Service from this repository:
- Build Command: `npm ci`
- Start Command: `npm start`
- No environment variables are required.
